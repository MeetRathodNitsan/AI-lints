name: AI Code Lint

on:
  pull_request:
    branches: ["master"]
  push:
    branches: ["master"]

jobs:
  ai-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -r requirements.txt
          .venv/bin/pip install requests fastapi uvicorn

      # Step 1: Start the FastAPI server in the background 🚀
      - name: Start AI Linting Server
        run: |
          # Replace 'main:app' with the correct module and app name for your FastAPI application.
          # The '&' runs the process in the background.
          .venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000 &
          # Wait a few seconds to ensure the server is fully started.
          sleep 5

      - name: Get changed Python files
        run: |
          # Get the list of changed files and replace newlines with a single space
          CHANGED_FILES=$(git diff --name-only b355c8b31d9320f24d70a3e80b4a4e51fdd2d91b e3e3eba601095d7ce58849b78e101d5174474615 | grep '\.py$' | tr '\n' ' ')

          # Check if any files were found before writing to the environment file
          if [[ -n "$CHANGED_FILES" ]]; then
            echo "CHANGED_FILES=$CHANGED_FILES" >> $GITHUB_ENV
          fi
        shell: /usr/bin/bash -e {0}


      - name: Run AI Lint
        if: env.CHANGED_FILES != ''
        run: |
          echo "[DEBUG] Files to lint: $CHANGED_FILES"
          for f in $CHANGED_FILES; do
            # The script should now be able to connect to the server
            .venv/bin/python ai_lint.py "$f"
          done

      - name: Check for unstaged changes
        run: |
          git_status=$(git status --porcelain)
          if [[ -n "$git_status" ]]; then
            echo "❌ Files were fixed automatically. Please commit the changes."
            git diff
            exit 1
          else
            echo "✅ All files already clean."
          fi